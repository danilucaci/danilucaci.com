import React from "react";

import SiteNavMenu from "./SiteNavMenu/SiteNavMenu";

const SiteNav = () => {
  return (
    <nav className="o-site__nav">
      <SiteNavMenu />
    </nav>
  );
};

export default SiteNav;
