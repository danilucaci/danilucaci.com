import React from "react";

import { StyledPlaceholder, StyledPlaceholderInner } from "./styles";

function DribbblePostPlaceholder() {
  return (
    <StyledPlaceholder>
      <StyledPlaceholderInner />
    </StyledPlaceholder>
  );
}

export default DribbblePostPlaceholder;
